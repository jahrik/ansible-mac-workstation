---
install: true
time_zone: US/Pacific

# base packages (installed via homebrew)
base_packages:
  - fd
  - git
  - htop
  - jq
  - ripgrep
  - tree
  - wget

# ZSH (passed to jahrik.zsh)
editor: nvim
lang: en_US.UTF-8
python_force_color: 1
ansible_force_color: 1
